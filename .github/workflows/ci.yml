name: HTML CSS JS Check

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main


jobs:

  validate:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Check project files
        run: |
          echo "Checking HTML CSS JS files..."
          ls
          echo "Validation completed successfully"
